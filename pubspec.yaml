name: flutter_svg_animations_with_rive_tutorial
description: Flutter SVG Animations With Rive Tutorial.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rive: ^0.12.4
  cupertino_icons: ^1.0.2
  flare_flutter: ^3.0.0
  audioplayers: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets